# 🚀 快速启动指南

## 一键启动/重启服务

```bash
# 重启所有服务（推荐）
./restart-services.sh

# 或使用完整命令
./restart-services.sh restart
```

## 📋 常用命令

```bash
# 查看服务状态
./restart-services.sh status

# 启动所有服务
./restart-services.sh start

# 停止所有服务
./restart-services.sh stop

# 重启所有服务
./restart-services.sh restart
```

## 🔄 开发模式（自动重启后端）

### 方式一：使用自动监控脚本（推荐）

```bash
# 终端1: 启动自动监控（监控后端代码变化，自动重启）
python3 auto-restart-backend.py

# 终端2: 手动启动前端（前端通常不需要频繁重启）
cd frontend && npm run dev
```

### 方式二：手动重启

每次修改后端代码后，运行：

```bash
./restart-services.sh restart
```

## 📝 注意事项

1. **首次运行**：脚本会自动检查并安装缺失的依赖
2. **日志文件**：所有日志保存在 `logs/` 目录
3. **端口占用**：脚本会自动处理端口占用问题
4. **自动监控依赖**：使用 `auto-restart-backend.py` 需要安装 `watchdog`：
   ```bash
   pip3 install watchdog
   ```

## 🎯 推荐工作流程

### 开发时

```bash
# 1. 启动自动监控（一个终端）
python3 auto-restart-backend.py

# 2. 启动前端（另一个终端）
cd frontend && npm run dev

# 3. 修改后端代码，自动重启
# 修改前端代码，前端会自动热重载
```

### 生产部署时

```bash
# 使用手动管理脚本
./restart-services.sh start
```

## 🐛 故障排除

如果服务无法启动：

1. 查看日志：`tail -f logs/backend.log` 或 `tail -f logs/frontend.log`
2. 检查端口：`lsof -i:8000` 和 `lsof -i:3000`
3. 手动启动测试：`cd backend && python3 -m uvicorn app.main:app --reload`

